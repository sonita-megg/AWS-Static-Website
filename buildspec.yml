version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Initializing project...
      - npm install

  build:
    commands:
      - echo Build started on `date`
      - npm run clean
      - npm run build

artifacts:
  files:
    - '**/*'
