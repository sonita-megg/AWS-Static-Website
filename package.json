{
  "name": "vacation-destinations-site",
  "version": "1.0.0",
  "description": "A static website showcasing vacation destinations with waterfalls and hot springs.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building the static site...' && mkdir -p dist && rsync -av --progress --exclude dist ./ dist/ && echo 'Build complete.'",
    "test": "echo 'No tests specified'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {}
}

